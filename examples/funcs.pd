proc increment_x(x: Integer) => Integer {
	x + 1
}

make a = increment_x(5);
make b = increment_x(a);

proc sum(x: Integer, y: Integer) => Integer {
	x + y
}

make c = sum(a, b);

proc random(x: Integer) => Integer {
	make b = x * 2;
	make a = b + 6;
	a - 2
}

make d = random(c);